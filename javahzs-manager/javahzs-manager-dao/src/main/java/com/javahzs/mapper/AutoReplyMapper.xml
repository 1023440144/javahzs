<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.javahzs.mapper.AutoReplyMapper">
    <select id="selectById" parameterType="Integer" resultType="AutoReply">
        SELECT * FROM auto_reply WHERE id = #{id}
    </select>
</mapper>